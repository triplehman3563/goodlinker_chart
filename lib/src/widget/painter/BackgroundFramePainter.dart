import 'package:flutter/material.dart';

class BackgroundFramePainter extends CustomPainter {
  @override
  void paint(Canvas canvas, Size size) {}

  @override
  bool shouldRepaint(BackgroundFramePainter oldDelegate) {
    return false;
  }
}
